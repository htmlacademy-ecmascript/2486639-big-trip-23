//! const getById = (items, id) => items.find((item) => item.id === id); //? а как по производительности?

//! const getByType = (items, id) => items.find((item) => item.id === id); //? а как по производительности?

//! getValueOf...

//! без оферов нужно убрать блок блок!

//! временно

//! временно выводим несколько событий

//! временно выводим форму редактирования

//! временно. сохранить то что будет диспользоваться в других методах.

//! от event? мало что педается

//! очень похожи FiltersView и SortingView, после наследования передалать

//! очень похожи с FiltersView

//! очень похожи с SortingView

//! прибрать!

//! разбить на файлы и сложить в папку util

//! с пустым списком нужно убрать весь блок и вывести сообщение! должен подойти MessageView <p class="trip-events__msg">Click New Event to create your first point</p>
