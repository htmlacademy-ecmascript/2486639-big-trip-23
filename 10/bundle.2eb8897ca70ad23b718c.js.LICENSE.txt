//! trip -> events?

//! Одинаково у всех презенторов, можно выделить в абстарктный презентор

//! временно

//! временно - скорее всего нужно сделать init()

//! временно, как будет готова фильтрация, то отфильтровать this.#events.filter(filterEvents(this.#currentFilterType))

//! временно, разный текст в зависмости от фильтра, и обработка нажатий

//! выше есть такие же две строки...

//! если не было событий, то нужно отрисовать шатку сортировки, но она в другом презенторе и скорее всего будет полная отрисовка

//! и сортировать и фильтровать дабавленное новое событие

//! как то покрасивее обойти при добавлении DEFAULT_EVENT.destination === null

//! можно собрать и один раз в конструкторе, сделать в util

//! нужно будет вызывать и при изменении фильтра, добавлении нового события

//! нужно будет вызывать при изменении данных

//! пока не стал делать static parseEventToState(event)

//! после добавления нового события пересчитать фильтры и отрисовать заново, если не все фильтры были активны

//! посмотреть ТЗ, что делать если уже открыто добавление

//! посмотреть в ТЗ, если собитий, то нет компонета сортировки. но наверное нет смысла его рисовать, т.к. можно отменить добавление

//! посмотреть по ТЗ какие требования к новому событию

//! похожий вызов отрисовки сообщения, может сделать функцию

//! почитать в ТЗ как считать сумму!

//! при измении пунтка назначения, можно заменить информацию, если по ТЗ не нужно обновлять destinations с сервера

//! применить dayjs isBefore isAfter

//! применить фильтр и сортировку, или сбросить все и применить сортировку по дням

//! проверить что now, не старое, наверное необходимо перерысовывать при определенных условиях и now передавать с основного перезентора

//! сделать (events) => {} и для проверок и для фильтрации, сортивки!

//! скорее всего будет полная отрисовка заново и придеться все удалить

//! считать либо тут - взять все данные или в модели

//! такое название в настройках, а linter ругаеться на camelCase

//! тут добавить проверку, что пункт назначения не выбран

//! тут нужно вызать пересчет Info через основного презентора

//! у каждого фильтра свое сообщение в ТЗ поискать

//! удалить, если не будет использоваться нигде кроме конструктора
